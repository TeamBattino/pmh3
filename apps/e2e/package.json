{
  "name": "@pfadipuck/e2e",
  "private": true,
  "scripts": {
    "test:e2e": "docker compose -f ../../docker-compose.e2e.yml run --rm --build e2e-runner; EXIT=$?; docker compose -f ../../docker-compose.e2e.yml down -v; exit $EXIT"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
