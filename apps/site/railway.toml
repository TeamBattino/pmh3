[build]
dockerfilePath = "apps/site/Dockerfile"
watchPatterns = ["/apps/site/**", "/packages/puck-web/**", "/packages/graphics/**"]

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
